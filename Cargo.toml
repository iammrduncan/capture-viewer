[package]
name = "capture-viewer"
version = "0.1.0"
edition = "2021"
description = "A tiny native macOS HDMI capture-card viewer"
license = "MIT"
build = "build.rs"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = true
